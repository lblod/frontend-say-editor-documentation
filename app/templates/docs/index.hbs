<h1>Overview</h1>

<div class="card">
  <div class="card-header">
    <h2 class="card-header-title">Integrating the editor as an Ember.js addon</h2>
    <a href="#" class="card-header-icon" aria-label="more options">
      <span class="icon">
        <i class="fa fa-circle-o" aria-hidden="true"></i>
      </span>
    </a>
  </div>
  <div class="card-content">
    <h3 class="is-4 subtitle">Your Ember.js app needs say</h3>
    <p>
      Given an existing ember-app, we'll show you how to integrate the say-editor in it. Check out the
      <LinkTo
        @route="docs.core.from-repository"
        @models={{array "lblod" "ember-rdfa-editor"}}>
        documentation
      </LinkTo>
      <br> <br>
      To see a real-world implementation, check out
      <LinkTo
        @route="docs.implementations.from-repository"
        @models={{array "lblod" "app-gelinkt-notuleren"}}>Gelinkt Notuleren</LinkTo>. <br>
      Many of the documented plugins are used in this project. <br>
      It will also give you a good idea of the possible user interactions with the say-editor.
    </p>
  </div>
</div>

<div class="card">
  <div class="card-header">
    <h2 class="card-header-title">Integrating the editor as a javascript package.</h2>
    <a href="#" class="card-header-icon" aria-label="more options">
      <span class="icon">
        <i class="fa fa-plug" aria-hidden="true"></i>
      </span>
    </a>
  </div>
  <div class="card-content">
    <h3 class="is-4 subtitle">Take a look at the following approaches!</h3>
    <p>
      Convinced by the capabilities of the say-editor, but don't want to use Ember.js?<br>
      Don't worry, we got you! There is still a way to use the editor in a framework-agnostic way.<br>
      You can integrate is as an external javascript library using <LinkTo
                                                                     @route="docs.core.from-repository"
                                                                     @models={{array "lblod" "frontend-embeddable-notule-editor"}}>Embeddable</LinkTo>.
    </p>
  </div>
</div>
